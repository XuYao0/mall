package org.example.mall.config;

import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI配置类
 * 用于配置Swagger文档的基本信息
 * 
 * @author Mall
 * @version 1.3
 */
@Configuration
public class OpenApiConfig {
    
    // SpringDoc会自动配置，这里可以添加自定义配置
    // 等SpringDoc依赖下载完成后，我们可以添加更多配置
    
}
